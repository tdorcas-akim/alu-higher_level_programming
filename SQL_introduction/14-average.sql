-- This script computes the average score of all records in the second_table
-- of the hbtn_0c_0 database. The result is displayed in a column named 'avera
-- ge'.

SELECT AVG(score) AS average
FROM second_table;
